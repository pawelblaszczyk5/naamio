# @naamio/schema

Package responsible for storing schema of various entities and domain objects across the Naamio apps. It uses `effect` schema which can be reused across frontend and backend and also used to store additional annotations.
