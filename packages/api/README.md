# @naamio/api

Package responsible for storing API schema, implemented with Effect. It allows safe consumption and `HttpApiClient` creation. On the other hand it forces backend to implement API matching the schema.
